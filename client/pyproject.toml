[project]
name = "burnscope-client"
version = "0.5.0"
description = "BurnScope client: per-fire Claude statusline + long-lived Codex app-server daemon, pushing snapshots to an ESP32."
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27",
  "zeroconf>=0.130",
]

[project.scripts]
burnscope = "burnscope_client.cli:main"

[dependency-groups]
dev = [
  "pytest>=8",
  "pytest-asyncio>=0.23",
  "respx>=0.21",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/burnscope_client"]
