#pragma once

/*
 * Compile-time WiFi credentials for Phase 1 bring-up.
 *
 * Copy this file to `wifi_creds.h` (gitignored) and fill in the SSID and
 * password of the 2.4 GHz network the device should join. Phase 2 replaces
 * this path with captive-portal provisioning into NVS.
 */

#define BURNSCOPE_WIFI_SSID     "your-ssid-here"
#define BURNSCOPE_WIFI_PASSWORD "your-password-here"
