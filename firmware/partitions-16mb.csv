# Name,   Type, SubType,  Offset,   Size,     Flags
# 16 MB layout for the Waveshare AMOLED-1.43 board (full chip).
# OTA slots are sized at 5 MB each (~3.3× current app) so we don't
# have to revisit flash sizing as the firmware grows. The remainder
# is mounted as a LittleFS `storage` volume for persistent assets
# (pixel-aging maps, future logs/manifests). NVS / otadata / phy_init
# offsets are identical to partitions-8mb.csv so a layout migration
# preserves the TOFU pairing slot.
nvs,      data, nvs,      0x9000,   0x6000,
otadata,  data, ota,      0xf000,   0x2000,
phy_init, data, phy,      0x11000,  0x1000,
ota_0,    app,  ota_0,    0x20000,  0x500000,
ota_1,    app,  ota_1,    0x520000, 0x500000,
storage,  data, littlefs, 0xa20000, 0x5E0000,
